export const SESSION_COOKIE = "portal_session";

// Filename marker for "Required Information" forms re-uploaded through the portal.
// Used to identify (and only display) portal-uploaded forms among a record's
// attachments, so unrelated internal attachments are never shown to the client.
export const REQUIRED_INFO_PREFIX = "Required Info - ";
