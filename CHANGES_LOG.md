# Changes Log

| Change / Update | Time Taken | Avg Good Developer |
| --------------- | ---------- | ------------------ |
| Added `/api/widget/upload-attachment` proxy endpoint (CORS open) so the Zoho CRM widget can upload files server-side — accepts `submissionLogId` + `file`, returns `attachmentId` | ~5 min | ~30 min |
| Added applicants setup flow — first-time login now redirects to `/applicants` page (two-step form: count selector → name fields). Saves names joined by `;` to `Applicants_Listing` on submission log (creates log if none exists). Middleware updated to auth-guard `/applicants`. Portal `page.js` redirects to applicants if `Applicants_Listing` is unset. New files: `applicants/page.js`, `applicants/ApplicantsForm.jsx`, `api/applicants/route.js` | ~25 min | 3–4 hours |
| Dynamic applicant tabs — portal now shows one Documents tab per applicant (e.g. "Dani's Documents | Ana's Documents | Messages") based on `Applicants_Listing`. Document status and upload history are scoped per applicant via `Submitted_For` field. File state is tracked per-applicant so selecting files on one tab is independent of another. Upload API now writes `Submitted_For` to each `Document_Uploads` subform row. | ~20 min | 2–3 hours |
| Fixed two bugs in applicants flow: (1) "Continue to Portal" button briefly re-enabled after success — removed `setSubmitting(false)` from `finally`, now only called on error paths. (2) Portal immediately redirected back to `/applicants` after first submission — Zoho search index delay caused `searchRecords` to return null on newly created log; fixed by passing `?lid={submissionLogId}` in redirect URL so portal uses `getRecord` by ID directly, bypassing search. | ~10 min | 45 min |
| Section approval now driven by `Section_Approvals` CRM field instead of uploaded file statuses. Top-level doc status ("APPROVED") and Front/Back slot labels now read from `Section_Approvals[docName].section`, `.front`, `.back`. Upload zone stays hidden only when section is explicitly approved by admin. | ~10 min | 30 min |
| Admin Reference Documents — admins can upload files (with optional message) to the `Admin_Uploads` subform on the Submission Log from the CRM widget; portal now shows a per-document amber "Reference Documents from Admin" section inside each accordion with the admin's message (if any) and View/Save buttons per file. Download route updated to support `?download=true&filename=...` for forced-save behaviour. | ~20 min | 2–3 hours |
| Migrated `middleware.js` → `proxy.js` (Next.js 16 file-convention rename). Renamed exported `middleware` function to `proxy`; `config.matcher` unchanged. Clears the deprecation warning shown on `next dev` startup. | ~2 min | 10 min |
| Added "Instructions" tab as the first tab on the portal (per client request). Default-selected on load. Covers: portal overview, navigation, status meanings (NOT SUBMITTED / PENDING / APPROVED / REJECTED), upload steps, reference docs from admin, Messages tab, password & session timeout. Shifted applicant tabs to indices `1..N` and Messages to `applicants.length + 1`; submit button now hidden on both Instructions and Messages tabs. | ~10 min | 1 hour |
