# Changes Log

| Change / Update | Time Taken | Avg Good Developer |
| --------------- | ---------- | ------------------ |
| Per-applicant scoping for broker-requested docs: a documentRequirements entry may carry an optional `forApplicants` array (applicant display names); rows with it render only on those applicants' tabs, rows without it stay universal. Added `isReqForApplicant` helper and filtered the applicant-tab render loop by it. Contract shared with the CRM widget which writes the key | ~5 min | ~30 min |
| Hide "Required Information" form upload button once a form has been received (client may upload only once) | ~3 min | ~20 min |
| Move Option 1 & 2 (application form intro + download/re-upload + online webform) out of Instructions into a new "Application Form" tab in 2nd position; reindex applicant/Messages tabs | ~6 min | ~30 min |
| Embed Zoho webform inline (iframe) in Option 2 instead of "Open the online form" button, so client submits without leaving the portal; kept ?id=recordId prefill | ~4 min | ~20 min |
| Project-wide mobile responsiveness pass: scrollable portal tabs, non-overlapping responsive header (Change Password button), responsive h4 title, DocumentItem status no longer squeezed by long names, explicit viewport meta; verified login at 375px | ~15 min | ~1.5 hr |
| Scope admin reference uploads to each applicant's own tab by filtering Admin_Uploads on Uploaded_For === applicantName (was shown on every applicant tab) | ~5 min | ~25 min |
| Align portal with the widget's new per-applicant Section_Approvals schema (`{ "<Applicant>": { "<Section>": true } }`): a document row shows APPROVED (and its upload zone locks) only when the broker has signed off that section for that specific applicant, no longer from a shared per-section flag. Added normalizeSectionApprovals with legacy-format migration (old `{Section:{section/front&back:true}}` read as approved for every applicant); DocumentItem now takes a single `sectionApproved` boolean (per-side front/back approval flags removed — one sign-off covers both sides). Contract shared with the CRM widget which writes the field | ~10 min | ~1.5 hr |
| Hide Option 2 (inline Zoho webform) on the Application Form tab at the client's request — commented out the Part 3 block together with its `webformUrl` const and the EditNoteIcon import, and swapped the Part 1 intro from two-option to single-option copy so it no longer points at a webform that isn't rendered. Both originals kept inline as comments so restoring is an uncomment | ~5 min | ~20 min |
| Multi-language portal (English / Spanish / German / Dutch) driven by the CRM `Client_Language` field. Added `lib/i18n` — locale resolver plus four dictionaries (115 keys each) written as native mortgage-portal copy in formal register (usted / Sie / u), not literal translations: possessives restructured per language (`John's documents` → `Documentación de John` / `Unterlagen von John`), and market-correct status vocabulary (ES *en revisión*, DE *in Prüfung / freigegeben*, NL *in behandeling / afgekeurd*). New `I18nProvider` context + `<Rich>` (renders `**bold**`) so emphasis can move with word order. Localised login, applicants wizard, change-password, all portal tabs, DocumentItem, AdminUploadsCard and MessagesPanel, plus locale-aware dates/times and CRM-derived labels (approval status, scan sides, requirement badge). Auth/applicants API routes now return stable error `code`s so server-side errors localise too. Resolution order: `Preferred_Language` wins if set, else `Client_Language` with English winning when tagged, else English. Broker-authored template text (document names/instructions) deliberately left as written | ~2 hr | ~2–3 days |
| Fix the portal tab strip overflowing once labels are translated. Measured in-browser: at MUI's default uppercase 14px + 16px padding the five tabs needed 761px (en) / 845 (nl) / 892 (de) / 920 (es) against ~768px available, so the strip flipped to scroll mode — which then reserved another 80px for arrows (dropping usable width to 688) and left a ~40px gutter from the disabled left arrow. Switched tabs to sentence case with tighter padding (uppercase is materially wider), and shortened the two long Spanish tab labels (`Formulario de solicitud` → `Formulario`, `Documentación de X` → `Documentos de X`, with the instructions copy that names those tabs updated to match). All four locales now fit with 68–144px headroom and no arrows; mobile still scrolls as before | ~20 min | ~1 hr |
