# Changes Log

| Change / Update | Time Taken | Avg Good Developer |
| --------------- | ---------- | ------------------ |
| Reworked first tab into the new 3-part "Required Information" flow — Part 1 (Instructions) done: replaced generic portal intro with personalized "Hi {FirstName}," greeting (`greetingName` derived from `clientName`, falls back to "there"), the client's required-info explanation, and the "two options" list (download & re-upload the form / fill the webform). Folded the still-relevant navigation tips below under a "Using the rest of the portal" heading. Added JSX anchors for Part 2 (preuploaded document) and Part 3 (embedded webform). | ~10 min | 45 min |
| Part 2 (Preuploaded document) — "Option 1" section in the first tab: Download button for the static form (`/public/credit-application-form.pdf`) + a re-upload control (file picker → "Upload completed form"). New endpoint `POST /api/required-info/upload` saves the filled file to the **related record's (Deal's) Attachments** via existing `uploadAttachment(module, recordId, file)`; session-guarded (verifies cookie's module+recordId match before writing). Success/error surfaced through the existing snackbar. NOTE: actual PDF binary still needs to be dropped into `/public`. | ~15 min | 1.5 hours |
| Part 2 polish — redesigned "Option 1" as a two-step card (coloured header, numbered Step 1 Download / Step 2 Upload) and added a persistent **"Form(s) received"** list with green check, filename and upload date. Persistence: re-uploaded files are stored with a `"Required Info - "` filename prefix (`REQUIRED_INFO_PREFIX` in constants); new `listAttachments(module, recordId)` (v8 needs `?fields=`) reads the record's attachments in `page.js`, filters by that prefix (so unrelated internal attachments are never shown to the client), strips the prefix for display, and passes `requiredInfoUploads` to the portal. `router.refresh()` after upload. `listAttachments` made non-fatal (returns `[]` on error) so it can't crash the portal. Friendly download filename via `download="Hipoteken Application Form.pdf"`. | ~25 min | 2 hours |
| Part 3 (Online webform) — "Option 2" card matching Option 1's style (coloured header + intro text + button). Button "Open the online form" links to the Zoho public form in a new tab (`target="_blank" rel="noopener noreferrer"`), with the **Deal ID passed as the `?id=` param** (`webformUrl` built from `recordId`, URL-encoded) so submissions can be tied back to the record. No iframe/embed per client preference. | ~10 min | 45 min |
